{
  "private": true,
  "engines": {
    "pnpm": ">=8.6.5"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "watch": "turbo run watch --filter=./package",
    "dev:html": "turbo run dev --filter=./examples/html",
    "build": "turbo run build",
    "format": "turbo run format --filter=./package",
    "build:package": "turbo run build --filter=./package",
    "upload:package": "turbo run upload --filter=./package",
    "clean": "turbo run clean --filter=./package"
  },
  "devDependencies": {
    "turbo": "^1.10.15"
  }
}
